{
  "name": "jenkins-cicd-demo",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "testecho": "echo \"Error: no test specified\" && exit 1",
    "test": "jest --ci --reporters=default --reporters=jest-junit --verbose --forceExit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "supertest": "^7.0.0"
  },
  "jest-junit": {
    "outputDirectory": "./test-results",
    "outputName": "junit.xml"
  }
}
